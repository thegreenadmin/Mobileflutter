git_url(ENV["MATCH_GIT_URL"] || "https://github.com/thegreenadmin/mobileapp-certificates.git")
storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, development, enterprise

app_identifier(["com.thegreenmall"])
username(ENV["FASTLANE_USER"] || ENV["APPLE_ID"])

# For more information about the Matchfile, see:
# https://docs.fastlane.tools/actions/match/
